# Rust build artifacts
/target/
fuzz/target/
/.embuild/

# User configuration — contains secrets, never commit
/config.toml

# Generated OTA firmware image — regenerate from the release build, do not commit
/smsgate-ota.bin

# PlatformIO artifacts (C++ sibling branches leak into working tree)
.pio/

# IDE
.vscode/
.idea/
docs/superpowers/

# OS
.DS_Store
Thumbs.db

# Tool data
.gsd/
.gsd-id
.bg-shell/

# C++ legacy (never commit — Rust uses config.toml)
src/secrets.h
